export interface INews {
    id: number;
    title: string;
    preview: string;
    content: string;
    image: string;
}

export interface IContacts {
    country: string;
    city: string;
    phone: string;
    email: string;
    website: string;
}